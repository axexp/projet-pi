{% extends 'commande.index.html.twig' %}
{% block title %} Votre Commande{% endblock %}


{% block body %}
<div class="card">
  <div class="row">
      <div class="col-md-8 cart">
          <div class="title">
              <div class="row">
                  <div class="col"><h4><b>liste de commandes en cours</b></h4></div>
                  <div class="col align-self-center text-right text-muted"></div>
              </div>
          </div>    
       {#   one item of comamnde #}
          {% for product in comisfounds %}
               
               
             <div class="row border-top border-bottom">
              <div class="row main align-items-center">
                  <div class="col-2"><img class="img-fluid" src="https://th.bing.com/th/id/OIP.xXYEk2OO86qqfUwv_UGKEwHaDt?w=644&h=322&rs=1&pid=ImgDetMain"></div>
                  <div class="col">
                      <div class="row text-muted">Commande ID :  {{ product.id }}</div>
                      <div class="row">Panier  ID :{{ product.idpanier.id}}</div>
                  </div>
                  <div class="col">
                      <a href="#">-</a><a href="#" class="border">{{ product.etat }}</a><a href="#">+</a>
                  </div>
                  <div class="col">{{ product.total()}} tnd<span class="close">&#10005;</span></div>
              </div>
          </div>
           {% endfor %} 
                  {# <a href="{{ path('app_panier', {'id': product.id}) }}" class="btn btn-primary"><i class="bi bi-cart-plus"></i> Ajouter</a> #}

          <div class="back-to-shop"> <a href="{{ path('app_panier')}}"><span class="text-muted">  BACK TO CART</span></a></div>
      </div>
         <div class="col-md-4 summary">
                <div><h5><b>Summary</b></h5></div>
                <hr>
                <div class="row">
                    {# <div class="col" style="padding-left:0;">{{}}</div> #}
                    <div class="col text-right">&euro; 132.00</div>
                </div>
                <form>
                    <p>SHIPPING</p>
                    <select><option class="text-muted">Standard-Delivery- &euro;5.00</option></select>
                    <p>ENTER YOUR ADRESS</p>
                    <input id="code" placeholder="Enter your Adress">
                </form>
                <div class="row" style="border-top: 1px solid rgba(0,0,0,.1); padding: 2vh 0;">
                    <div class="col">TOTAL PRICE</div>
                    {# <div class="col text-right"> {{comisfounds.total}}</div> #}
                </div>
                            {# <div class="back-to-shop"> <a href="{{ path('app_panier')}}"><span class="text-muted">  BACK TO CART</span></a></div> #}
                    {# <a href="{{path('app_add',{'id': product.idproduct.getId()})}}" class="btn btn-primary">Ajouter</a> #}

                {# <a href="{{ path('commande_change_status',{'idp':objpanier.id})}}"a> #}
                   <button  class="btn">CHECKOUT</button>
                </a>
            </div>
  </div>
  
</div>
{% endblock %}
