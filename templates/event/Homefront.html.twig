{% extends 'base_front.html.twig' %}

{% block title %}HOME PAGE{% endblock %}

{% block navigate %}
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav ms-auto py-0">

                    <a href="{{ path('app_homefront',  {'id':user.id}) }}" class="nav-item nav-link active">Home</a>
                    <a href="{{ path('app_homefront',  {'id':user.id}) }}" class="nav-item nav-link">About</a>
                    <a href="{{ path('app_homefront',  {'id':user.id}) }}" class="nav-item nav-link">Services</a>
                    <a href="{{ path('app_showb',  {'id':user.id}) }}" class="nav-item nav-link ">Events</a>
                    


                    <div class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Pages</a>
                        <div class="dropdown-menu m-0">
                            <a href="" class="dropdown-item">Destination</a>
                            <a href="" class="dropdown-item">Booking</a>
                            <a href="" class="dropdown-item">Travel Guides</a>
                            <a href="" class="dropdown-item">Testimonial</a>
                            <a href="" class="dropdown-item">404 Page</a>
                        </div>
                    </div>
                    <a href="{{ path('app_homefront',  {'id':user.id}) }}" class="nav-item nav-link">Contact</a>
                </div>
                <!--<a href="" class="btn btn-primary rounded-pill py-2 px-4">Register</a>-->
            </div>
{% endblock %}

{% block body %}

{% endblock %}
